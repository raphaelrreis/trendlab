apiVersion: v1
kind: Service
metadata:
  name: {{ include "trendlab.fullname" . }}
  labels:
    {{- include "trendlab.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "trendlab.selectorLabels" . | nindent 4 }}
